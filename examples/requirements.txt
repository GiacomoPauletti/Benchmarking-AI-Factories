# Requirements for running examples
# Install with: pip install -r requirements.txt

# HTTP requests
requests>=2.28.0

# HuggingFace datasets (optional, for loading benchmark datasets)
datasets>=2.14.0

# PyArrow (required by datasets for parquet files)
pyarrow>=12.0.0
